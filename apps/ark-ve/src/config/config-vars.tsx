import { LayoutPanelTop } from 'lucide-react';

import type { GlobalConfig } from '@/types/global-config';

export const globalConfig: GlobalConfig = {
  appTitle: 'ArkUI + Vanilla Extract',
  appIcon: <LayoutPanelTop />,
};
